[tool.poetry]
name = "pygt1000"
version = "0.2.0"
description = "Library to interact with a Boss GT-1000/GT-1000CORE over MIDI"
authors = ["Julien Desfossez <j@jdfz.org>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/jdesfossez/pygt1000"
keywords = ["midi", "boss", "gt1000", "gt-1000", "gt-1000core", "guitar", "bass"]

[tool.poetry.dependencies]
python = "^3.10"
python-rtmidi = "^1.5.8"

[tool.poetry.group.dev.dependencies]
python-lsp-server = "^1.11.0"
pylsp-mypy = "^0.6.8"
python-lsp-ruff = "^2.2.2"
ruff = "^0.6.1"


[tool.poetry.group.test.dependencies]
pytest = "^8.3.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
# Import pygt1000 straight from the source tree so the suite runs without
# installing the package (the compiled python-rtmidi dependency is stubbed in
# tests/conftest.py, so the wire binding need not be built to run tests).
pythonpath = ["."]
